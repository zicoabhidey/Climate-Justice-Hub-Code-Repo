<style>
    #wp-custom-grid-789 {
        font-family: "Martina Plantijn", serif;
        background-color: #f0f2f5;
        color: #1c1e21;
        transition: padding 0.3s ease;
        box-sizing: border-box;
    }


    html.modal-open,
    body.modal-open {
        overflow: hidden !important;
    }

    /* --- Keyframes for Floating Effect (Global is OK) --- */
    @keyframes float {
        0% {
            transform: translateY(0px);
            box-shadow: 0 5px 15px 0px rgba(0,0,0,0.1);
        }
        50% {
            transform: translateY(-8px);
            box-shadow: 0 25px 15px 0px rgba(0,0,0,0.05);
        }
        100% {
            transform: translateY(0px);
            box-shadow: 0 5px 15px 0px rgba(0,0,0,0.1);
        }
    }

    /* --- Photo Grid Styles (Prefixed) --- */
    #wp-custom-grid-789 .photo-grid-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 15px; /* Adjust the space between photos */
        max-width: 1600px;
        margin: 0 auto; /* Center the grid */
    }
    
    #wp-custom-grid-789 .grid-item {
        position: relative; /* Anchor for the absolute caption */
        /*animation: float 4s ease-in-out infinite;*/
        border-radius: 12px; /* Match the image radius */
        overflow: hidden; /* Keep the scaling image contained */
        cursor: pointer;
    }

    #wp-custom-grid-789 .grid-item img {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Ensures images fill their container without distortion */
        border-radius: 12px; /* Apply radius to all corners */
        transition: transform 0.3s ease;
        display: block;
    }

    /* Image hover effect */
    #wp-custom-grid-789 .grid-item:hover img {
        transform: scale(1.05); 
    }

    #wp-custom-grid-789 .grid-caption {
        position: absolute;
        /* Corrected to cover the entire item */
        top: 0; 
        left: 0;
        width: 100%;
        height: 100%;
        
        background: rgba(46, 107, 36, 0.7); /* Dark semi-transparent background */
        color: #ffffff; /* White text */
        
        /* Added flex properties to center text */
        display: flex;
        align-items: center;
        justify-content: center;
        
        font-size: 1.1em;
        text-align: center; /* Fallback */
        
        padding: 15px; /* Padding for a better look */
        margin: 0; /* Reset default margin */
        
        /* Hide by default */
        opacity: 0;
        visibility: hidden;
        
        /* Smooth transition */
        transition: opacity 0.3s ease, visibility 0.3s ease;
        
        box-sizing: border-box; /* Ensure padding doesn't break layout */
        
        /* Match parent's border radius */
        border-radius: 12px;
    }

    /* Show caption on hover */
    #wp-custom-grid-789 .grid-item:hover .grid-caption {
        opacity: 1;
        visibility: visible;
    }


    /* --- Modal (Pop-up Window) Styles (Prefixed) --- */
    #wp-custom-grid-789 .photo-modal {
        display: none; /* Hidden by default */
        position: fixed; /* Stays in place, covers the whole screen */
        z-index: 10000; /* Increased z-index for WP admin bar */
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: hidden; /* Disable scrolling on the modal container */
        background-color: rgba(0, 0, 0, 0.85); /* Black background with opacity */
        
        /* Flexbox for centering the content */
        justify-content: center;
        align-items: center;
    }

    #wp-custom-grid-789 .photo-modal-content {
        background-color: #ffffff;
        margin: auto;
        padding: 25px;
        border-radius: 10px;
        width: 90%;
        max-width: 800px; /* Increased max-width for better video viewing */
        max-height: 90vh; /* Set a max height */
        overflow-y: auto; /* Allow scrolling ONLY within the content box */
        position: relative;
        box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        animation: fadeIn 0.4s;
        /* Apply the font inside the modal too */
        font-family: "Martina Plantijn", serif; 
    }

    /* --- Video Responsiveness Styles (UPDATED) --- */
    /* Applies to video/G Drive video embeds (16:9 ratio) */
    #wp-custom-grid-789 .gdrive-wrapper,
    #wp-custom-grid-789 .video-wrapper {
        position: relative;
        padding-bottom: 56.25%; /* 16:9 aspect ratio (9/16 * 100 = 56.25) */
        height: 0;
        overflow: hidden;
        margin-bottom: 20px;
        border-radius: 8px;
        background-color: #000;
    }

    /* UPDATED: Style specifically for audio players (reduced width and centered) */
    #wp-custom-grid-789 .audio-wrapper {
        position: relative;
        padding-bottom: 6%; /* Reduced height for audio player */
        padding-top: 8%; 
        height: 0;
        overflow: hidden;
        border-radius: 8px;
        background-color: #eee;
        
        /* New centering and width constraints */
        max-width: 500px; /* Reduced width for a cleaner audio player look */
        margin: 0 auto 20px auto; /* Center horizontally and add bottom margin */
    }

    #wp-custom-grid-789 .gdrive-wrapper iframe,
    #wp-custom-grid-789 .video-wrapper iframe,
    #wp-custom-grid-789 .audio-wrapper iframe { /* Target all wrapper iframes */
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        border: 0;
    }
    /* --- END Video Responsiveness Styles --- */


    /* --- Modal Animation Classes (Prefixed) --- */
    #wp-custom-grid-789 .photo-modal-content.modal-closing {
        animation: fadeOut 0.4s forwards;
    }

    /* Keyframes are global, so no prefix needed */
    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: scale(0.9);
        }
        to {
            opacity: 1;
            transform: scale(1);
        }
    }
    
    @keyframes fadeOut {
        from {
            opacity: 1;
            transform: scale(1);
        }
        to {
            opacity: 0;
            transform: scale(0.9);
        }
    }
    
    /* Modal Close Button (Prefixed) */
    #wp-custom-grid-789 .photo-modal-close {
        color: #aaa;
        position: absolute;
        top: 10px;
        right: 20px;
        font-size: 28px;
        font-weight: bold;
        transition: color 0.2s ease;
    }

    #wp-custom-grid-789 .photo-modal-close:hover,
    #wp-custom-grid-789 .photo-modal-close:focus {
        color: #111;
        text-decoration: none;
        cursor: pointer;
    }
    
    /* Styles for content inside the modal (Prefixed) */
    #wp-custom-grid-789 .modal-body h2,
    #wp-custom-grid-789 .modal-body h3 {
        margin-top: 0;
        /* Ensure font is applied to headings */
        font-family: "Martina Plantijn", serif; 
    }
    #wp-custom-grid-789 .modal-body img {
        width: 100%;
        border-radius: 8px;
        margin-bottom: 15px;
    }
    
    /* --- RESPONSIVE STYLES (Prefixed) --- */
    @media screen and (max-width: 768px) {
        #wp-custom-grid-789 .photo-grid-container {
            /* Switch to a single column on smaller screens */
            grid-template-columns: 1fr;
        }
        #wp-custom-grid-789 h1,
        #wp-custom-grid-789 h3 {
            font-size: 1.75rem; /* Make heading smaller on mobile */
        }
    }

</style>


<div id="wp-custom-grid-789">

    

<div class="photo-grid-container">
    
    

<div class="grid-item" data-modal-target="#photoModal7">
            <img src="https://centerforthehumanities.org/wp-content/uploads/2025/10/Tier-III-1-scaled.jpg" alt="Tier III 1">
            <p class="grid-caption">The Point CDC Truck Count Group Photo Spring 2025</p>
        </div>

    

<div class="grid-item" data-modal-target="#photoModal8">
            <img src="https://centerforthehumanities.org/wp-content/uploads/2025/10/Tier-III-2-scaled.jpg" alt="Tier III 2">
            <p class="grid-caption">Lehman Climate Justice Journalism Initiative</p>
        </div>

    

<div class="grid-item" data-modal-target="#photoModal9">
            <img src="https://centerforthehumanities.org/wp-content/uploads/2025/10/Tier-III-3.png" alt="Tier III 3">
            <p class="grid-caption">Climate Justice Documentaries for CUNY TV</p>
        </div>
    </div>

    

<div id="photoModal7" class="photo-modal">
        <div class="photo-modal-content">
            <span class="photo-modal-close">&times;</span>
            <div class="modal-body">
            <h3>The Point CDC Truck Count Group Photo Spring 2025</h3>
            &nbsp;
            <div class="gdrive-wrapper">
                    <!-- Responsive GDrive iframe -->
                    <iframe src="https://drive.google.com/file/d/1nu6_Tv1VbUK1xp-ZS45hZTV1-Ozrmf00/preview" allow="autoplay" allowfullscreen></iframe>
                </div>
            <div class="gdrive-wrapper">
                <!-- Responsive GDrive iframe -->
            <iframe src="https://drive.google.com/file/d/1B7Oosl_7TAYFxi9QN-LvkjH8aXFeZni0/preview" allow="autoplay" allowfullscreen></iframe>
            </div>
<div class="audio-wrapper">
                    <!-- Responsive GDrive iframe -->
                    <iframe src="https://drive.google.com/file/d/1690tZnQfJaGTbeG5QPWhqaNSBp9bXXFq/preview" allow="autoplay" allowfullscreen></iframe>
                    </div>
                <p>During the spring of 2024, a partnership with The Point CDC and CCNY Prof. Lauren Wang’s Climate Justice Class engaged in a community call to action around Last Mile Facilities.  The class engaged in site visits, fieldwork, and stakeholder interviews to collect truck traffic counts at last-mile warehouse sites in Hunts Point identified by the community and stories about the community's experience with truck /heavy duty vehicle traffic. Findings were shared with the Hunts Point and CUNY Community.  </p>
            </div>
        </div>
    </div>

    

<div id="photoModal8" class="photo-modal">
        <div class="photo-modal-content">
            <span class="photo-modal-close">&times;</span>
            <div class="modal-body">
                    <h3>Lehman Climate Justice Journalism Initiative</h3>
                    &nbsp;
                    <!-- Class changed to audio-wrapper to reduce height, and CSS updated to reduce width and center -->
                    <div class="audio-wrapper">
                    <!-- Responsive GDrive iframe -->
                    <iframe src="https://drive.google.com/file/d/1690tZnQfJaGTbeG5QPWhqaNSBp9bXXFq/preview" allow="autoplay" allowfullscreen></iframe>
                    </div>
                    <p>In the fall of 2024, two students in Dr. Eileen Markey’s Journalism and Media Studies  are engaged in an independent project creating a five-episode podcast on the roots and future of the environmental justice movement in the Bronx. The podcast documents the modern movement from the battle against the medical waste incinerator in the 1990s to the campaigns to dismantle the Sheridan Expressway, restore the Bronx River and document poor air quality and contemporary battles by South Bronx NYC-EJA membersFor their independent Fall 2025 project, Dr. Eileen Markey’s Journalism and Media Studies Two students are producing a five-episode podcast. This podcast will explore the origins and future of the environmental justice movement in the Bronx, tracing its evolution from the 1990s battle against the medical waste incinerator to current campaigns. These contemporary efforts include dismantling the Sheridan Expressway, restoring the Bronx River, documenting poor air quality, and addressing ongoing struggles faced by South Bronx NYC-EJA members.</p>
            </div>
        </div>
    </div>

    

<div id="photoModal9" class="photo-modal">
        <div class="photo-modal-content">
            <span class="photo-modal-close">&times;</span>
            <div class="modal-body">
                    <h3>Climate Justice Documentaries for CUNY TV</h3>
                 &nbsp;
                 <div class="video-wrapper">
                        <!-- Responsive YouTube iframe -->
                    <iframe src="https://www.youtube.com/embed/1oq3EJRsALk?start=44" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                </div>
                    <p>In the fall of 2023, students from Dr. Shelly Eversley's Climate Justice class collaborated with GOLES to create short documentaries. These films highlight climate justice issues within New York City, presenting both problems and potential solutions.</p>
            </div>
        </div>
    </div>

</div> 

<script>

    (function() {

        // Check if this script has already run to avoid double-binding
        if (window.wpCustomGrid789Loaded) {
            return;
        }
        window.wpCustomGrid789Loaded = true;

        function initCustomGrid() {
            
            const gridWrapper = document.getElementById('wp-custom-grid-789');
            
            if (!gridWrapper) {
                return;
            }

            const modalTriggers = gridWrapper.querySelectorAll('[data-modal-target]');
            const closeButtons = document.querySelectorAll('#photoModal7 .photo-modal-close, #photoModal8 .photo-modal-close, #photoModal9 .photo-modal-close');
            const allModals = document.querySelectorAll('#photoModal7, #photoModal8, #photoModal9');
            const htmlEl = document.documentElement;

            modalTriggers.forEach(trigger => {
                trigger.addEventListener('click', () => {
                    const modalId = trigger.getAttribute('data-modal-target');
                    // We must select the modal by its ID globally
                    const modal = document.querySelector(modalId); 
                    if (modal) {
                        modal.style.display = 'flex';
                        htmlEl.classList.add('modal-open'); 
                        document.body.classList.add('modal-open');
                    }
                });
            });

            function closeModal(modal) {
                if (!modal) return;
                const modalContent = modal.querySelector('.photo-modal-content');
                if (modalContent) {
                    modalContent.classList.add('modal-closing');
                }
                

                const videoIframes = modal.querySelectorAll('iframe');
                videoIframes.forEach(iframe => {

                    const tempSrc = iframe.src;
                    iframe.src = tempSrc; 
                });
                // 

                setTimeout(() => {
                    modal.style.display = 'none';
                    if (modalContent) {
                        modalContent.classList.remove('modal-closing');
                    }
                    // Only remove modal-open class if no other modals are open
                    const anyModalOpen = Array.from(allModals).some(m => m.style.display === 'flex');
                    if (!anyModalOpen) {
                        htmlEl.classList.remove('modal-open');
                        document.body.classList.remove('modal-open');
                    }
                }, 400); 
            }

            closeButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modal = button.closest('.photo-modal');
                    if (modal) {
                        closeModal(modal);
                    }
                });
            });

            // Add click-off-to-close functionality for all modals
            allModals.forEach(modal => {
                modal.addEventListener('click', (event) => {
                    if (event.target === modal) {
                        closeModal(modal);
                    }
                });
            });
        }

        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', initCustomGrid);
        } else {
            initCustomGrid();
        }

    })(); 
</script>